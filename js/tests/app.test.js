import list from "../app";

describe("Should render javascript exercise", () => {
  test("should return an array of posts and users", () => {
    // return letter.get().then(([usersData, postsData]) => {
    //   // expect(usersData, postsData).toBe([]);
    // });
  });

  test("should return new array of posts and users and print it", () => {
    // expect(list).toBe([]);
  });
});
